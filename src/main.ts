/**
 * Point d'entrée principal — Domaine de Valdor
 * Initialisation de l'application Vue 3
 */
import { createApp } from 'vue'
import App from './App.vue'

// Création et montage de l'application
const app = createApp(App)
app.mount('#app')
